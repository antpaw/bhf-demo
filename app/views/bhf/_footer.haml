- if @start_time
  %p.state= t('bhf.footer.state', time: (Time.now-@start_time).round(2)).html_safe

%p.info= t('bhf.footer.info').html_safe

%p.copy= t('bhf.footer.copy').html_safe

- # it's a copied file with the database reset button

%p= link_to 'Reset Database', main_app.reset_database_path(return_to: request.original_url), class: :alt_button
